using Cf.Pattern;
using UnityEngine;
using UnityEngine.Pool;

public class EnemyHelper : MonoBehaviour, IObjectPoolAble<EnemyHelper>
{
    public void SetPool(ObjectPool<EnemyHelper> pool)
    {
        
    }
}
