using UnityEngine;

public enum GeoType
{
    Unknown,
    Tee,
    Fairway,
    Bunker,
    Fringe,
    Green,
    Rough,
    Road,
    Ob,
}

public class GeoHelper : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
